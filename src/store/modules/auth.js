import * as authApi from '../../api/auth'

const state = {
  user: null,
}

// actions 可异步
const actions = {}

// mutations 必须同步
const mutations = {}

export default {
  namespaced: true,
  state,
  // getters,
  actions,
  mutations
}
